PROGRAM test
BEGIN

	INT i, j,k;
	
	FUNCTION VOID main()
	BEGIN
        INT u;
	k := 0;
	u:=9;	
    FOR (i := 0; i < 4; i := i+1)
        k := k+1;
        u:=10;
	FOR (j := 0; j < 5; j := j+1)
		k := k+1;
        ENDFOR
	
    ENDFOR

	WRITE(k);
        WRITE(u);
	END
END
