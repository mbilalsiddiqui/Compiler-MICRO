PROGRAM test
BEGIN
	STRING str := "test";
	INT a,b,c,g,h,i,j,k,l,p,o,r,u,y;


	FUNCTION INT main(INT a)
	BEGIN
	
	a := 0;
	b := 0;
     i := 10;
	
     FOR (j := 10; b <= (i - 1); )
          b := b + 1;
          FOR (b := b + 1; TRUE; a := a + 1)
          ENDFOR
     ENDFOR

     WRITE(a);

	RETURN a+b;
	END
END

